class AppConstants {
  static const String appName = 'Sistema de Gestión Mortuaria';
  static const bool enableLogs = true;
}

class StorageKeys {
  static const String token = 'sgm.auth.token';
  static const String user = 'sgm.auth.user';
  static const String sessionTime = 'sgm.auth.session_time';
}

class Breakpoints {
  static const double mobile = 600;
  static const double tablet = 1024;
}

class SessionConfig {
  static const int timeoutMinutes = 720;
}